name: Deploy
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
        with:
          lfs: true
      - name: Deno Deploy
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
        run: deno deploy --token ${{ secrets.DENO_DEPLOY_TOKEN }}
